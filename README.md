# Merge Excel Files

Browser-based spreadsheet utilities for merging, splitting, and converting Excel, CSV, and JSON files.

The production build creates independent indexable tool pages and processes uploaded files locally in the browser.

## Development

```bash
npm install
npm run dev
```

## Quality checks

```bash
npm run typecheck
npm run build
```
